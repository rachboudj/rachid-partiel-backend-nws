{% extends 'base.html.twig' %}

{% block title %}CommandeMateriel{% endblock %}

{% block body %}
    <h1>CommandeMateriel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande_materiel.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ commande_materiel.quantite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commande_materiel_index') }}">back to list</a>

    <a href="{{ path('app_commande_materiel_edit', {'id': commande_materiel.id}) }}">edit</a>

    {{ include('commande_materiel/_delete_form.html.twig') }}
{% endblock %}
