<form method="post" action="{{ path('app_commande_materiel_delete', {'id': commande_materiel.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commande_materiel.id) }}">
    <button class="btn">Delete</button>
</form>
